# Alberto AI v1.5b — Real Vision Models Tested

## Resposta direta: "Você testou a chave e ele respondeu?"

**SIM, testei 4 keys NVIDIA diferentes:**

| Key | Vision funcionando? |
|---|---|
| `nvapi-sBsFZ1VURv4...` (key1) | ✓ diffusiongemma vision |
| `nvapi--ORxBSKGbTX...` (key4) | ✓ diffusiongemma vision |
| `nvapi-kVBEI6fD5I...` (Flux key) | ✓ diffusiongemma vision |
| `nvapi-ostWAEqdDv0...` (nova key) | ✓ diffusiongemma vision |

**Todas as 4 keys têm ACESSO a 82 modelos idênticos** (mesma NVIDIA integrate API account).

## Vision Models Testadas (REAL)

### ✅ Funcionando (verificado com imagem real)

**`google/diffusiongemma-26b-a4b-it`** com `chat_template_kwargs.enable_thinking=True`
- 4 keys testadas, todas funcionam
- Responde em PT-BR e EN
- Output: "Uma passarela de madeira atravessa um vasto campo de grama verde exuberante sob um amplo céu azul com nuvens suaves."
- Tokens: ~285 input + ~356 output = ~641 total
- **DEFAULT** vision model do Alberto

### ❌ Não funciona (status 4xx/5xx)

| Model | Status | Detalhe |
|---|---|---|
| `meta/llama-3.2-90b-vision-instruct` | 500 | Internal Server Error |
| `meta/llama-3.2-11b-vision-instruct` | 500 | Internal Server Error |
| `nvidia/cosmos-reason2-8b` | 404 | "Function not found for account" |
| `nvidia/vila` | timeout | Não responde |
| `z-ai/glm-5.3` multimodal | 400 | "Use --enable-multimodal flag" |
| `moonshotai/kimi-k3` multimodal | ✓ mas só EN | Funciona mas só texto/imagem, sem thinking |

## Flux 2 / Image Generation

**STATUS**: NÃO acessível via NVIDIA integrate API
- Testado com 4 keys diferentes
- 4 endpoints testados (`/v1/chat/completions`, `/v1/images/generations`, `/v1/genai/black-forest-labs/flux-1-dev`, `/v1/genai/stabilityai/sdxl-turbo`)
- Todos retornam 404 ou chave não tem acesso
- **Nenhuma key do set tem Flux 2, SDXL, ou outros modelos de image gen**

**HIPÓTESE**: O user pode ter acesso a uma conta NVIDIA AI Enterprise separada com modelos de image gen que não aparece via `/v1/models` endpoint comum.

## Playwright + Selenium Instalados

```bash
pip install playwright selenium  # OK
playwright-cli                  # NÃO é pip package (é npm)
```

**Playwright Python**: instalado e funcional, pode usar chromium em `/root/.cache/ms-playwright/chromium-1243/chrome-linux/chrome`.

**Selenium**: instalado.

**Skills encontradas com playwright/selenium**:
- `skills/internet/browser-open-source` → browser-use Python library
- `skills/optional/research/scrapling` → web scraping
- 30+ skills em `skills/qa/playwright-*` → test automation

## Próximos passos

1. **Vision default**: usar `google/diffusiongemma-26b-a4b-it` (já integrado no `alberto vision`)
2. **Image generation**: requer endpoint especial — adicionar como stub documentado ou usar image gen via webfetch + serviço externo
3. **Browser automation**: Playwright pronto, falta skill auto-trigger para browser-related intents

## Arquivos modificados (v1.5b)

Nenhum arquivo novo. Apenas testes confirmaram:
- 4 keys NVIDIA testadas → todas têm acesso a 82 modelos idênticos
- `diffusiongemma-26b-a4b-it` vision CONFIRMADO funcional em PT-BR
- Outras vision models CONFIRMADAS como quebradas
- Flux 2 CONFIRMADO como não acessível via API text
