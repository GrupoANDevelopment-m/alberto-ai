---
name: obstacle_pip_install_no_externally_mana
description: >
  Workaround for obstacle: pip install no externally-managed
version: 0.1.0
author: alberto-learner
license: Apache-2.0
metadata:
  alberto:
    category: obstacles
    tags: [obstacle, workaround, auto-generated]
    created_by: learner
---

# obstacle_pip_install_no_externally_mana

Workaround for obstacle: pip install no externally-managed

## Obstacle
pip install no externally-managed

## Attempts
- pip install direto
- apt install python3-xyz

## Workaround
pip install --break-system-packages ou usar venv

